---
project:
  server:
    application:
      name: local.{{app.url}}
      alias: local.{{app.url}}
    database:
      name: local.db.{{app.url}}
      alias: local.db.{{app.url}}
      path: /usr/share/adminer/adminer/
    log_php: /var/log/php5/
    admin: support@{{app.url}}
  repository:
    url: https://'{{github_account}}':'{{github_password}}'@github.com/Link4LEAD/ChekChekChek.git
    version: draco

security:
  allow:
    script:
      -
    style:
      -
    img:
      -
    font:
      -
